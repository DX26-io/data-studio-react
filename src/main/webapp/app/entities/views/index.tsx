import React from 'react';
import { Switch } from 'react-router-dom';

const Routes = ({ match }) => (
  <>
    <Switch>// Routes to be build for accessing the views directly</Switch>
  </>
);

export default Routes;
