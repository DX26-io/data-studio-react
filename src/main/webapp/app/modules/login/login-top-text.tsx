import React from 'react';
import Typography from '@material-ui/core/Typography';

// TODO change this to logo
const LoginTopText = props => (
  <Typography variant="h6" className="dx26-font-bold" style={{ position: 'absolute', right: '3%', top: '4%' }}>
    DX26
  </Typography>
);

export default LoginTopText;
